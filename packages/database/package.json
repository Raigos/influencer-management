{
  "name": "@influencer-management/database",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "prisma": "^6.0.1"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "6.0.1"
  }
}